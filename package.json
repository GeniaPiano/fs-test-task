{
  "name": "fs-test-task",
  "version": "1.0.0",
  "private": true,
  "description": "Full Stack Test Task",
  "scripts": {
    "dev:fe": "cd fe && yarn dev",
    "dev:be": "cd be && yarn dev",
    "seed": "cd be && yarn seed",
    "docker:up": "docker compose -f docker-compose.dev.yml up",
    "docker:down": "docker compose -f docker-compose.dev.yml down"
  },
  "author": "Illia S",
  "license": "ISC",
  "workspaces": [
    "fe",
    "be"
  ]
}
